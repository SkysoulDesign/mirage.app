<Page android:actionBarHidden="true">
	<!-- <StackLayout orientation="vertical"> -->
	<GridLayout rows="*, *" >
		<Image src="~/image/soap.png" stretch="fill" />

		<!-- register sign in -->
        <GridLayout row="1" columns="*, auto, auto, *" rows="*"> 
        	<Button text="Register" col="1" tap="tapRegister" cssClass="register-login-button" />
			<Button text="Sign in" col="2" tap="tapLogin" cssClass="register-login-button" />
        </GridLayout>

       <!--  <!-- register -->
        <!-- <GridLayout visibility="{{ selectedScreen===1 ? 'visible' : 'collapsed' }}" columns="*, *, *, *" rows="*">
			<label text="Sign in" col="1" tap="tapLogin" />
        </GridLayout>

        login
        <GridLayout visibility="{{ selectedScreen===2 ? 'visible' : 'collapsed' }}" columns="*, *, *, *" rows="*">
			<Button text="Main" col="1" tap="tapmain" />
        </GridLayout> --> 
	</GridLayout>
</Page>